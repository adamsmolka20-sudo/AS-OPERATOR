# components/three

React Three Fiber / drei scene components: canvases, meshes, materials, lighting rigs,
and camera controllers. All files here that touch the R3F `<Canvas>` tree must be
Client Components (`"use client"`) since WebGL cannot render on the server.
