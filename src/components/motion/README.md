# components/motion

Reusable Framer Motion primitives (reveal-on-scroll, staggered lists, page
transitions). Wrap animation config here once and reuse it, instead of
inlining `motion.div` variants across sections.
