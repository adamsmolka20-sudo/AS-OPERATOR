# components/sections

Composed, page-level sections (e.g. hero, feature blocks) built by combining
`components/ui` primitives, `components/motion` wrappers, and `components/three` scenes.
Sections are assembled into routes under `app/`, not the other way around.
